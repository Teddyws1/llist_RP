const listText = `abanar, abdominal, abracar2, abracar, abracointura2, abracointura, ajoelhar, alongar2, alongar3, alongar4, alongar5, alongar6, alongar7, alongar, amem, anotar, aqc2, aqc, arrumar, atm, bale, bateria, bebado2, bebado3, bebado4, bebado5, bebado6, bebado7, bebado8, bebado, beijar, beijo2, beijo, binoculos, bixa, boboalegre2, boboalegre3, boboalegre4, boboalegre5, boboalegre, bolsa2, bolsa3, bolsa4, bolsa, bonzao, bora, boxe2, boxe, cortaessa, cachorro, cafe2, cafe, caixa2, caixa3, caixa, camera, camera2, cansado, capo, carona, celebrar, celular, cerveja, churrasco, chuva2, chuva, cocada2, cocada, consertar, continencia, creu, cruzar10, cruzar2, cruzar3, cruzar4, cruzar5, cruzar6, cruzar7, cruzar8, cruzar9, cruzar, cuidar2, cuidar3, cuidar, colher,  dancar100, dancar101, dancar102, dancar103, dancar104, dancar105, dancar106, dancar107, dancar108, dancar109, dancar10, dancar110, dancar111, dancar112, dancar113, dancar114, dancar115, dancar116, dancar117, dancar118, dancar119, dancar11, dancar120, dancar121, dancar122, dancar123, dancar124, dancar125, dancar126, dancar127, dancar128, dancar129, dancar12, dancar130, dancar131, dancar132, dancar133, dancar134, dancar135, dancar136, dancar137, dancar138, dancar139, dancar13, dancar140, dancar141, dancar142, dancar143, dancar144, dancar145, dancar146, dancar147, dancar148, dancar149, dancar14, dancar150, dancar151, dancar152, dancar153, dancar154, dancar155, dancar156, dancar157, dancar158, dancar159, dancar15, dancar160, dancar161, dancar162, dancar163, dancar164, dancar165, dancar166, dancar167, dancar168, dancar169, dancar16, dancar170, dancar171, dancar172, dancar173, dancar174, dancar175, dancar176, dancar177, dancar178, dancar179, dancar17, dancar180, dancar181, dancar182, dancar183, dancar184, dancar185, dancar186, dancar187, dancar188, dancar189, dancar18, dancar190, dancar191, dancar192, dancar193, dancar194, dancar195, dancar196, dancar197, dancar198, dancar199, dancar19, dancar200, dancar201, dancar202, dancar203, dancar204, dancar205, dancar206, dancar207, dancar208, dancar209, dancar20, dancar210, dancar211, dancar212, dancar213, dancar214, dancar215, dancar216, dancar217, dancar218, dancar219, dancar21, dancar220, dancar221, dancar222, dancar223, dancar224, dancar225, dancar226, dancar227, dancar228, dancar229, dancar22, dancar230, dancar231, dancar232, dancar233, dancar234, dancar235, dancar236, dancar237, dancar238, dancar239, dancar23, dancar240, dancar241, dancar242, dancar243, dancar244, dancar245, dancar246, dancar247, dancar248, dancar249, dancar24, dancar250, dancar251, dancar252, dancar253, dancar254, dancar255, dancar256, dancar25, dancar260, dancar261, dancar262, dancar263, dancar26, dancar27, dancar28, dancar29, dancar2, dancar30, dancar31, dancar32, dancar33, dancar34, dancar35, dancar36, dancar37, dancar38, dancar39, dancar3, dancar40, dancar41, dancar42, dancar43, dancar44, dancar45, dancar46, dancar47, dancar48, dancar49, dancar4, dancar50, dancar51, dancar52, dancar53, dancar54, dancar55, dancar56, dancar57, dancar58, dancar59, dancar5, dancar60, dancar61, dancar62, dancar63, dancar64, dancar65, dancar66, dancar67, dancar68, dancar69, dancar6, dancar70, dancar71, dancar72, dancar73, dancar74, dancar75, dancar76, dancar77, dancar78, dancar79, dancar7, dancar80, dancar81, dancar82, dancar83, dancar84, dancar85, dancar86, dancar87, dancar88, dancar89, dancar8, dancar90, dancar91, dancar92, dancar93, dancar94, dancar95, dancar96, dancar97, dancar98, dancar99, dancar9, dancar, debrucar2, debrucar3, debrucar4, debrucar5, debrucar, dedo2, dedo3, dedo, deitar2, deitar3, deitar4, deitar5, deitar6, deitar7, deitar8, deitar, digitar2, digitar, dinheiro, dj2, dj3, dj4, dj, dormir2, dormir4, dormir5, dormir, escorar, escorar2, escorar3, encostar10, encostar11, encostar12, encostar2, encostar3, encostar4, encostar5, encostar6, encostar7, encostar8, encostar9, encostar, escorregar2, escorregar, escutar, espernear, esquentar, estatua2, estatua, explodir, fedo, fera, ferido2, ferido, galinha, gang2, gang, heroi, inspec10, inspec11, inspec12, inspec13, inspec14, inspec15, inspec16, inspec2, inspec3, inspec4, inspec5, inspec6, inspec7, inspec8, inspec9, inspec, investigar, joia2, joia, karate2, karate, lavar, lero2, lero3, lero, ligar, livro, lixo, louco, malicia, mamamia, mapa, martelo, mecanico2, mecanico3, mecanico4, mecanico5, mecanico, meditar4, meditar2, meditar3, meditar, medo, megaphone, meleca2, meleca3, meleca, mexer, mic2, mic3, mic4, mic, money, morto2, morto, musica2, musica3, musica4, musica, naruto2, naruto, nervoso, no, ok, olhar2, olhar, onda, palmas2, palmas3, palmas4, palmas5, palmas6, palmas, parachoque, passaro, peace2, peace, peitos, pensar2, pensar, pescar2, pescar, pirueta2, pirueta, placa2, placa3, placa, plantar2, plantar, pneu, poledance1, poledance2, poledance, porta, postura2, postura3, postura, prancheta2, prancheta, procurar, punheta, puto2, puto3, puto, radio2, radio, rebolar, rock2, rock, selfie2, selfie, limpar, senha, sentar10, sentar11, sentar12, sentar13, sentar14, sentar15, sentar16, sentar17, sentar18, sentar19, sentar20, sentar21, sentar22, sentar2, sentar3, sentar4, sentar5, sentar6, sentar8, sentar9, sexo2, sexo3, sexo4, sexo5, sexo6, sexo7, sexo, sinalizar,soprador3, soprador2, soprador, spiderman, striper, suicidio2, suicidio, swat2, swat3, swat4, swat5, swat6, swat7, swat8, swat9, swat, tablet, tiltado, triste, trotar, varrer, rastejar, verificar, wait2, wait3, wait4, wait5, wtf2, wtf3, wtf4, wtf, xiu, yoga2, yoga, yeah, assobiar, fodase, taco, onda, alongar3, alongar4, alongar5, lutar3, ameacar, ameacar2, rosa, buque, britadeira, flexao, fuma, fuma1, fuma2, fuma3, fuma4, malhar, malhar2, tragar, pullover, airguitar, airsynth, unhas, mandarbeijo, cruzarbraco, cruzarbraco2, lutar2, lutar, mochila, exercício, comer2, comer, comer3, beber, beber2, beber3, beber4, beber5, beber6, beber7, pano, pano2, prebeber, prebeber2, prebeber2, superhero, argue, comeatmebro, type, bird, blowkiss, bringiton, chill, crawl, flip, flip2, inspct, morrer,`; // sua string original


/* =========================================================
   [INÍCIO] - PROCESSAMENTO DE DADOS E VARIÁVEIS GLOBAIS
   Descrição: Trata a string 'listText', cria os itens e
   gerencia o estado dos favoritos e aba atual.
   ========================================================= */
const animations = [...new Set(listText.split(',')
    .map(item => item.trim())
    .filter(item => item !== ''))]
    .sort((a, b) => a.localeCompare(undefined, { numeric: true, sensitivity: 'base' }));

let rawItems = listText.split(',').map(n => n.trim()).filter(n => n !== "");
let items = rawItems.map((name, i) => ({ name, id: i + 1 }));
let favorites = JSON.parse(localStorage.getItem('teddyFavs')) || [];
let currentTab = 'all';
let currentItem = null;
/* =========================================================
   [FIM] - PROCESSAMENTO DE DADOS
   ========================================================= */


/* =========================================================
   [INÍCIO] - FUNÇÃO DE RENDERIZAÇÃO (NÚCLEO DO SISTEMA)
   Descrição: Filtra, ordena, conta e desenha os cards na tela.
   ========================================================= */
function renderItems() {
    const grid = document.getElementById('gridItems');
    const search = document.getElementById('searchInput').value.toLowerCase();
    const sort = document.getElementById('sortOption').value;
    const clearAllBtn = document.getElementById('clearAllBtn');
    
    // Elemento do contador (certifique-se de ter o id="totalContador" no HTML)
    const contador = document.getElementById('totalContador');

    // Botão de limpar busca individual
    document.getElementById('clearBtn').style.display = search ? 'block' : 'none';
    
    // Escolha entre todos os itens ou favoritos
    let data = currentTab === 'all' ? [...items] : [...favorites];

    // Filtro de busca por nome ou ID
    if(search) {
        data = data.filter(i => 
            i.name.toLowerCase().includes(search) ||   
            i.id.toString() === search ||              
            i.id.toString().includes(search)           
        );
    }

    // Ordenação
    if(sort === 'alpha') {
        data.sort((a, b) => a.name.localeCompare(b.name));
    } else {
        data.sort((a, b) => b.id - a.id);
    }

    // ATUALIZAÇÃO DO CONTADOR
    if (contador) {
        contador.innerText = data.length;
    }

    // Botão de limpar todos os favoritos
    clearAllBtn.style.display = (currentTab === 'fav' && favorites.length > 0) ? 'block' : 'none';

    // Criação dos cards HTML
    grid.innerHTML = data.map(item => `
        <div class="card" onclick="openModal(${item.id})">
            ${currentTab === 'fav' ? `<i class="fas fa-trash-can delete-fav" onclick="event.stopPropagation(); removeFavorite(${item.id})"></i>` : ''}
            <strong>${item.name}</strong>
            <span>ID: ${item.id}</span>
        </div>
    `).join('');
}
/* =========================================================
   [FIM] - FUNÇÃO DE RENDERIZAÇÃO
   ========================================================= */


/* =========================================================
   [INÍCIO] - GERENCIAMENTO DE FAVORITOS
   Descrição: Funções para excluir um ou todos os favoritos.
   ========================================================= */
function clearAllFavorites() {
    if(confirm("Deseja limpar todos os favoritos?")) {
        favorites = [];
        localStorage.setItem('teddyFavs', JSON.stringify(favorites));
        renderItems();
    }
}

function removeFavorite(id) {
    favorites = favorites.filter(f => f.id !== id);
    localStorage.setItem('teddyFavs', JSON.stringify(favorites));
    renderItems();
}
/* =========================================================
   [FIM] - GERENCIAMENTO DE FAVORITOS
   ========================================================= */


/* =========================================================
   [INÍCIO] - CONTROLE DE MODAIS E SIDEBAR
   Descrição: Abre/fecha modais de detalhes e a barra lateral.
   ========================================================= */
function openTextModal(title, body) {
    document.getElementById('textModalTitle').innerText = title;
    document.getElementById('textModalBody').innerText = body;
    document.getElementById('textModal').style.display = 'flex';
}

function closeModal(id) { document.getElementById(id).style.display = 'none'; }
function toggleSidebar() { document.getElementById('sidebar').classList.toggle('active'); }
function clearSearch() { document.getElementById('searchInput').value = ''; renderItems(); }

function openModal(id) {
    if (document.getElementById('sidebar').classList.contains('active')) return;
    currentItem = items.find(i => i.id === id);
    document.getElementById('modalTitle').innerText = currentItem.name;
    document.getElementById('modalID').innerText = `#${currentItem.id}`;
    document.getElementById('itemModal').style.display = 'flex';
    updateFavBtn();
}

function updateFavBtn() {
    const isFav = favorites.find(f => f.id === currentItem.id);
    const btn = document.getElementById('favBtn');
    btn.innerHTML = isFav ? '<i class="fas fa-star-half-alt"></i> Remover' : '<i class="fas fa-star"></i> Favoritar';
    btn.style.background = isFav ? "var(--danger)" : "var(--primary)";
}

document.getElementById('favBtn').onclick = () => {
    const idx = favorites.findIndex(f => f.id === currentItem.id);
    idx > -1 ? favorites.splice(idx, 1) : favorites.push(currentItem);
    localStorage.setItem('teddyFavs', JSON.stringify(favorites));
    updateFavBtn();
    renderItems();
};
/* =========================================================
   [FIM] - CONTROLE DE MODAIS
   ========================================================= */


/* =========================================================
   [INÍCIO] - UTILITÁRIOS E ESTÉTICA
   Descrição: Troca de abas, cópia de texto e temas.
   ========================================================= */
function switchTab(tab) {
    currentTab = tab;
    document.getElementById('tabAll').classList.toggle('active', tab === 'all');
    document.getElementById('tabFav').classList.toggle('active', tab === 'fav');
    renderItems();
}

function copyCommand() {
    navigator.clipboard.writeText(`/e ${currentItem.name}`);
    const btn = document.querySelector('.btn-copy');
    btn.innerHTML = '<i class="fas fa-check"></i> Pronto!';
    setTimeout(() => btn.innerHTML = '<i class="fas fa-copy"></i> Copiar', 1000);
}

function toggleTheme() { document.body.classList.toggle('light-theme'); }
/* =========================================================
   [FIM] - UTILITÁRIOS E ESTÉTICA
   ========================================================= */


/* =========================================================
   [INÍCIO] - EVENTOS DE JANELA E INSTALAÇÃO (PWA)
   Descrição: Cliques fora do modal e registro de Service Worker.
   ========================================================= */
window.onclick = (e) => {
    const sidebar = document.getElementById('sidebar');
    if (e.target.classList.contains('modal-overlay')) closeModal(e.target.id);
    if (sidebar && sidebar.classList.contains('active') && !sidebar.contains(e.target) && e.target !== document.getElementById('menuBtn') && !e.target.closest('.modal-overlay')) {
        toggleSidebar();
    }
}

document.getElementById('searchInput').oninput = renderItems;

if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('sw.js').then(() => console.log("SW registrado!"));
}
/* =========================================================
   [FIM] - EVENTOS E INSTALAÇÃO
   ========================================================= */


/* =========================================================
   [INÍCIO] - SEGURANÇA E PROTEÇÃO
   Descrição: Bloqueios de clique direito, teclado, cópia e zoom.
   ========================================================= */
const CONFIG_PROTECAO = {
    bloquearCliqueDireito: false,
    bloquearTeclado: false,
    bloquearSelecao: false
};

(function() {
    document.addEventListener('contextmenu', (e) => CONFIG_PROTECAO.bloquearCliqueDireito && e.preventDefault());
    document.onselectstart = () => !CONFIG_PROTECAO.bloquearSelecao;
})();

// Bloqueio de Zoom Pinça e Ctrl+Scroll
document.addEventListener('touchstart', (e) => {
    if (e.touches.length > 1) e.preventDefault();
}, { passive: false });

document.addEventListener('wheel', (e) => {
    if (e.ctrlKey) e.preventDefault();
}, { passive: false });
/* =========================================================
   [FIM] - SEGURANÇA E PROTEÇÃO
   ========================================================= */


// Inicialização imediata ao carregar o script
renderItems();
// Faz o contador pulsar
const contador = document.getElementById('totalContador');
contador.classList.remove('bump');
void contador.offsetWidth; // Truque para resetar a animação
contador.classList.add('bump');

/* =========================================================
sobre atualização do sistema 
   ========================================================= */
  
   (function() {
    const startModal = () => {
        // Seletores por ID exclusivo
        const overlay = document.getElementById('un-modal-container');
        const openBtn = document.getElementById('un-btn-trigger');
        const closeBtn = document.getElementById('un-btn-confirm');
        const closeX = document.getElementById('un-close-x');

        if (!overlay || !openBtn) return;

        const open = () => { overlay.style.display = 'flex'; };
        const close = () => { overlay.style.display = 'none'; };

        openBtn.addEventListener('click', open);
        if (closeBtn) closeBtn.addEventListener('click', close);
        if (closeX) closeX.addEventListener('click', close);

        // Fecha se clicar fora do card
        overlay.addEventListener('click', (e) => {
            if (e.target === overlay) close();
        });
    };

    // Executa sem conflitos
    if (document.readyState === 'complete') {
        startModal();
    } else {
        window.addEventListener('load', startModal);
    }
})();
const modal = document.getElementById('un-modal-container');
const btnOpen = document.getElementById('un-btn-trigger');
const btnClose = document.getElementById('un-close-x');

// Abrir Modal
btnOpen.addEventListener('click', () => {
    modal.style.display = 'flex';
});

// Fechar ao clicar no X
btnClose.addEventListener('click', () => {
    modal.style.display = 'none';
});

// Fechar ao clicar fora do card (no fundo cinza)
window.addEventListener('click', (event) => {
    if (event.target === modal) {
        modal.style.display = 'none';
    }
});

/* =========================================================
 sistema de bloqueio de Scroll 
   ========================================================= */
   
  
   
   /* =========================================================
fim sobre atualização do sisitema 
   ========================================================= */
